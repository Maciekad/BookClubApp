import Header from './Header';
import Navigation from './Navigation';

const HomePage = () => {
    return(
        <div>
            <Header/>
            <Navigation/>
        </div>
    );
}

export default HomePage;